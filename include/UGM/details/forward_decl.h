#pragma once

namespace Ubpa {
	template<typename T, size_t N>
	struct bbox;
	template<typename T>
	struct euler;
	template<typename T, size_t N>
	struct hvec;
	template<typename T, size_t N>
	struct line;
	template<typename T, size_t N>
	struct mat;
	template<typename T>
	struct normal;
	template<typename T>
	struct plane;
	template<typename T, size_t N>
	struct point;
	template<typename T>
	struct quat;
	template<typename T, size_t N>
	struct ray;
	template<typename T>
	struct rgb;
	template<typename T>
	struct rgba;
	template<typename T, size_t N>
	struct scale;
	template<typename T>
	struct svec;
	template<typename T>
	struct transform;
	template<typename T, size_t N>
	struct triangle;
	template<typename T, size_t N>
	struct val;
	template<typename T, size_t N>
	struct vec;
}
